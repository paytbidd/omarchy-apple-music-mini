diff --git a/AppleMusicModel.js b/AppleMusicModel.js
index 0d8dd44..d482a33 100644
--- a/AppleMusicModel.js
+++ b/AppleMusicModel.js
@@ -62,14 +62,39 @@ function metadataText(value) {
     .replace(/</g, "‹").replace(/>/g, "›")
 }
 
-// Chromium normally publishes either an HTTPS cover or a canonical local
-// file URI. Reject every other Image source scheme, remote file authorities,
-// whitespace/control characters, and overlong URLs.
+// Chromium's MPRIS art dump is often 150px. Apple CDN URLs carry a size in
+// the path (`{w}x{h}bb.jpg` or `/60x60bb.jpg`); request a square large
+// enough for the miniplayer at HiDPI.
+var ARTWORK_EDGE = 1200
+
+function upgradeArtworkUrl(url) {
+  var value = String(url || "")
+  if (!value) return ""
+  if (value.indexOf("{w}") !== -1 || value.indexOf("{h}") !== -1) {
+    return value.split("{w}").join(String(ARTWORK_EDGE)).split("{h}").join(String(ARTWORK_EDGE))
+  }
+  return value.replace(/\/(\d+)x(\d+)([a-z]*)(\.[a-z0-9]+)(?:\?.*)?$/i,
+    function(match, width, height, suffix, ext) {
+      var edge = Math.max(Number(width), Number(height))
+      if (!(edge > 0) || edge >= ARTWORK_EDGE) return match
+      return "/" + ARTWORK_EDGE + "x" + ARTWORK_EDGE + suffix + ext
+    })
+}
+
+// Chromium normally publishes either an HTTPS cover or a /tmp dump of one.
+// Reject every other Image source, including Chromium's product logo — MPRIS
+// falls back to that application icon when Media Session metadata is cleared
+// on pause, which is not cover art.
 function artworkUrl(value) {
   var url = String(value || "")
   if (!url || url.length > 2048 || /[\u0000-\u0020\u007f]/.test(url)) return ""
-  if (/^https:\/\/[^/?#]+(?:[/?#]|$)/i.test(url)) return url
-  if (/^file:\/\/\/(?!\/)/i.test(url)) return url
+  if (/product_logo/i.test(url)) return ""
+  if (/\/usr\/(lib|share)\//i.test(url) && /chrom/i.test(url)) return ""
+  if (/^https:\/\/[^/?#]+(?:[/?#]|$)/i.test(url)) {
+    if (/(^|\.)gstatic\.com\/chrome/i.test(url)) return ""
+    return upgradeArtworkUrl(url)
+  }
+  if (/^file:\/\/\/tmp\//i.test(url)) return url
   return ""
 }
 
@@ -99,6 +124,14 @@ function historyLogKey(title, artist) {
 // title/artist pair so replaying a song moves it to the top instead of adding
 // another visible row. Title/artist also deduplicates legacy records that do
 // not carry a MusicKit playback descriptor.
+function historyField(entries, field) {
+  if (!entries || !entries.length) return ""
+  var entry = entries[0]
+  if (!entry || typeof entry !== "object") return ""
+  if (field === "art") return artworkUrl(entry.art)
+  return metadataText(entry[field])
+}
+
 function uniqueHistoryEntries(entries, cap) {
   var limit = Number(cap) || 0
   var result = []
@@ -332,8 +365,10 @@ if (typeof module !== "undefined") {
     bridgeIsActive: bridgeIsActive,
     upNextFromState: upNextFromState,
     metadataText: metadataText,
+    upgradeArtworkUrl: upgradeArtworkUrl,
     artworkUrl: artworkUrl,
     historyLogKey: historyLogKey,
+    historyField: historyField,
     uniqueHistoryEntries: uniqueHistoryEntries,
     historyPlaybackDescriptor: historyPlaybackDescriptor,
     parseHistoryLines: parseHistoryLines,
diff --git a/AppleMusicService.qml b/AppleMusicService.qml
index f1e45e6..4d71717 100644
--- a/AppleMusicService.qml
+++ b/AppleMusicService.qml
@@ -59,14 +59,23 @@ Item {
   readonly property var players: Mpris.players ? Mpris.players.values : []
   readonly property var activePlayer: Model.selectPlayer(players, browserPid)
   readonly property bool available: activePlayer !== null
-  readonly property bool hasMedia: available && (title !== "" || artist !== "")
   readonly property bool playing: available && activePlayer.isPlaying
-  readonly property string title: available
+  // Chromium often blanks Media Session on pause (or when the window is
+  // occluded). Keep the last real track while the dedicated window lives so
+  // the bar does not flash the Chromium product icon or an empty label.
+  property string heldTitle: ""
+  property string heldArtist: ""
+  property string heldAlbum: ""
+  readonly property string liveTitle: available
     ? Model.metadataText(activePlayer.trackTitle) : ""
-  readonly property string artist: available
+  readonly property string liveArtist: available
     ? Model.metadataText(activePlayer.trackArtist) : ""
-  readonly property string album: available
+  readonly property string liveAlbum: available
     ? Model.metadataText(activePlayer.trackAlbum) : ""
+  readonly property string title: liveTitle || (running ? (heldTitle || Model.historyField(recentTracks, "title")) : "")
+  readonly property string artist: liveArtist || (running ? (heldArtist || Model.historyField(recentTracks, "artist")) : "")
+  readonly property string album: liveAlbum || (running ? (heldAlbum || Model.historyField(recentTracks, "album")) : "")
+  readonly property bool hasMedia: title !== "" || artist !== ""
   // Chromium republishes MPRIS metadata several times per track switch, each
   // time pointing mpris:artUrl at a new /tmp file and deleting the previous
   // one within ~600ms. Binding an Image directly to that churn loads files
@@ -74,7 +83,12 @@ Item {
   // only once it stops changing, so only the surviving final file is used.
   readonly property string rawArtUrl: available
     ? Model.artworkUrl(activePlayer.trackArtUrl) : ""
-  property string artUrl: ""
+  property string settledArtUrl: ""
+  // MusicKit's CDN URL is 1200px; Chromium MPRIS is a ~150px /tmp dump.
+  readonly property string bridgeArtUrl: bridgeActive
+    ? Model.artworkUrl(bridgeState && bridgeState.artworkUrl) : ""
+  readonly property string artUrl: bridgeArtUrl || settledArtUrl ||
+    (running ? Model.historyField(recentTracks, "art") : "")
   readonly property real position: {
     positionRevision
     return available && activePlayer.positionSupported
@@ -100,6 +114,11 @@ Item {
     Quickshell.execDetached([launcherPath, "install"])
   }
 
+  function setSystemFont(enabled) {
+    Quickshell.execDetached([launcherPath, "set-font-pref", enabled ? "on" : "off"])
+    sendBridgeCommand({ action: "set-system-font", enabled: !!enabled })
+  }
+
   function sendBridgeCommand(command) {
     if (!bridgeActive) return
     commandSequence += 1
@@ -221,20 +240,31 @@ Item {
   // before the entry is written, and lastHistoryKey collapses the result.
   onTitleChanged: historyLogTimer.restart()
 
+  onLiveTitleChanged: if (liveTitle !== "") heldTitle = liveTitle
+  onLiveArtistChanged: if (liveArtist !== "") heldArtist = liveArtist
+  onLiveAlbumChanged: if (liveAlbum !== "") heldAlbum = liveAlbum
+  onRunningChanged: {
+    if (!running) {
+      heldTitle = ""
+      heldArtist = ""
+      heldAlbum = ""
+      settledArtUrl = ""
+    }
+  }
+
   onRawArtUrlChanged: {
     if (rawArtUrl === "") {
       artSettleTimer.stop()
-      artUrl = ""
-    } else {
-      artSettleTimer.restart()
+      return
     }
+    artSettleTimer.restart()
   }
 
   Timer {
     id: artSettleTimer
     interval: 600
     repeat: false
-    onTriggered: root.artUrl = root.rawArtUrl
+    onTriggered: root.settledArtUrl = root.rawArtUrl
   }
 
   Timer {
diff --git a/AppleMusicWidget.qml b/AppleMusicWidget.qml
index 94924a8..aaea2ca 100644
--- a/AppleMusicWidget.qml
+++ b/AppleMusicWidget.qml
@@ -18,6 +18,14 @@ BarWidget {
   readonly property bool showQueue: setting("showQueue", true)
   readonly property bool showRecentlyPlayed: setting("showRecentlyPlayed", true)
   readonly property bool showPlaybackModes: setting("showPlaybackModes", true)
+  readonly property bool systemFont: setting("systemFont", false)
+  property int artworkSizeOverride: 0
+  readonly property int artworkSize: {
+    var value = root.artworkSizeOverride > 0
+      ? root.artworkSizeOverride : Number(setting("artworkSize", 260))
+    if (!(value > 0)) value = 260
+    return Math.round(Math.max(180, Math.min(420, value)))
+  }
   // Nerd Font nf-md glyphs, resolved from CaskaydiaMono Nerd Font's cmap:
   // md-heart, md-heart_outline, md-thumb_down, md-thumb_down_outline
   readonly property string heartIcon: String.fromCodePoint(0xf02d1)
@@ -35,6 +43,8 @@ BarWidget {
   readonly property string clockOutlineIcon: String.fromCodePoint(0xf0465)
   readonly property string alertIcon: String.fromCodePoint(0xf0292)
   readonly property string autoplayIcon: String.fromCodePoint(0xf0ab5)
+  // nf-md-open-in-new — expand the miniplayer into the full web UI.
+  readonly property string openInNewIcon: String.fromCodePoint(0xf03cc)
   readonly property color popupForeground: bar ? bar.foreground : Color.foreground
   readonly property string popupFontFamily: bar ? bar.fontFamily : Style.font.family
   readonly property string trackLabel: {
@@ -56,6 +66,35 @@ BarWidget {
   function open() { popupOpen = true }
   function close() { popupOpen = false }
   function toggle() { popupOpen = !popupOpen }
+  function openFullPlayer() {
+    popupOpen = false
+    if (music) music.openAppleMusic()
+  }
+
+  onSystemFontChanged: if (music) music.setSystemFont(systemFont)
+
+  function toggleSystemFont() {
+    var next = !root.systemFont
+    Quickshell.execDetached([
+      "omarchy", "bar", "set", "iuliansafta.apple-music", "systemFont",
+      next ? "true" : "false", "--json"
+    ])
+    if (music) music.setSystemFont(next)
+  }
+
+  function setArtworkSize(value) {
+    var next = Math.round(Math.max(180, Math.min(420, Number(value))))
+    root.artworkSizeOverride = next
+    Quickshell.execDetached([
+      "omarchy", "bar", "set", "iuliansafta.apple-music", "artworkSize", String(next)
+    ])
+  }
+  readonly property int miniMaxHeight: {
+    var avail = popup.availableCardHeight
+    var cap = Math.max(640, root.artworkSize + 300)
+    if (avail > 0) return Math.min(cap, Math.round(avail * 0.7))
+    return cap
+  }
 
   // Last artwork URL that decoded successfully. Chromium hands MPRIS a new
   // /tmp artwork file several times per track switch and deletes the old
@@ -263,90 +302,246 @@ BarWidget {
     }
   }
 
+  component MiniButton: BorderSurface {
+    id: miniBtn
+    property string iconText: ""
+    property real iconSize: Style.font.icon
+    property bool enabled: true
+    property bool active: false
+    property string tooltipText: ""
+    property real horizontalPadding: Style.space(10)
+    property real verticalPadding: Style.space(8)
+    property color iconColor: root.popupForeground
+    signal clicked()
+
+    readonly property bool hot: buttonMouse.containsMouse && enabled
+    readonly property color ink: root.popupForeground
+
+    implicitWidth: glyph.implicitWidth + horizontalPadding * 2
+    implicitHeight: Math.max(Style.space(34), glyph.implicitHeight + verticalPadding * 2)
+    radius: Style.cornerRadius
+    opacity: enabled ? 1 : 0.4
+    color: !enabled ? "transparent"
+      : buttonMouse.pressed ? Style.pressedFillFor(ink, ink)
+      : (hot || active) ? Style.selectedFillFor(ink, ink)
+      : Style.normalFillFor(ink, ink)
+    borderSpec: !enabled ? Border.none()
+      : (hot || buttonMouse.pressed)
+        ? Border.controlSpec("hover-cursor", ink, ink)
+        : (active
+          ? Border.controlSpec("selected", ink, ink)
+          : Border.controlSpec("normal", ink, ink))
+
+    Behavior on color { ColorAnimation { duration: 80 } }
+
+    Text {
+      id: glyph
+      anchors.centerIn: parent
+      textFormat: Text.PlainText
+      text: miniBtn.iconText
+      color: miniBtn.iconColor
+      font.family: root.popupFontFamily
+      font.pixelSize: miniBtn.iconSize
+    }
+
+    MouseArea {
+      id: buttonMouse
+      anchors.fill: parent
+      hoverEnabled: true
+      enabled: miniBtn.enabled
+      cursorShape: enabled ? Qt.PointingHandCursor : Qt.ArrowCursor
+      Accessible.role: Accessible.Button
+      Accessible.name: miniBtn.tooltipText !== "" ? miniBtn.tooltipText : miniBtn.iconText
+      onClicked: miniBtn.clicked()
+      onContainsMouseChanged: {
+        if (!root.bar || miniBtn.tooltipText === "") return
+        if (containsMouse) root.bar.showTooltip(miniBtn, miniBtn.tooltipText)
+        else root.bar.hideTooltip(miniBtn)
+      }
+    }
+  }
+
+  component TrackRow: BorderSurface {
+    id: trackRow
+    property string titleText: ""
+    property string detailText: ""
+    property string trailingText: ""
+    property bool playable: true
+    signal activated()
+
+    readonly property bool hot: trackMouse.containsMouse
+
+    width: parent ? parent.width : 0
+    radius: Style.cornerRadius
+    implicitHeight: trackInner.implicitHeight + Style.space(16)
+    color: hot
+      ? Style.selectedFillFor(root.popupForeground, root.popupForeground)
+      : "transparent"
+    borderSpec: hot
+      ? Border.controlSpec("hover-cursor", root.popupForeground, root.popupForeground)
+      : Border.none()
+
+    Behavior on color { ColorAnimation { duration: 80 } }
+
+    Row {
+      id: trackInner
+      anchors.left: parent.left
+      anchors.right: parent.right
+      anchors.verticalCenter: parent.verticalCenter
+      anchors.leftMargin: Style.space(10)
+      anchors.rightMargin: Style.space(10)
+      spacing: Style.space(8)
+
+      Column {
+        width: parent.width - (trailingLabel.visible ? trailingLabel.implicitWidth + Style.space(8) : 0)
+        spacing: Style.space(1)
+
+        Text {
+          width: parent.width
+          text: trackRow.titleText
+          textFormat: Text.PlainText
+          color: root.popupForeground
+          font.family: root.popupFontFamily
+          font.pixelSize: Style.font.bodySmall
+          font.bold: trackRow.hot
+          elide: Text.ElideRight
+        }
+
+        Text {
+          width: parent.width
+          visible: trackRow.detailText !== ""
+          text: trackRow.detailText
+          textFormat: Text.PlainText
+          color: Qt.darker(root.popupForeground, trackRow.hot ? 1.2 : 1.5)
+          font.family: root.popupFontFamily
+          font.pixelSize: Style.font.caption
+          elide: Text.ElideRight
+        }
+      }
+
+      Text {
+        id: trailingLabel
+        visible: trackRow.trailingText !== ""
+        text: trackRow.trailingText
+        textFormat: Text.PlainText
+        color: Qt.darker(root.popupForeground, trackRow.hot ? 1.2 : 1.6)
+        font.family: root.popupFontFamily
+        font.pixelSize: Style.font.caption
+        anchors.verticalCenter: parent.verticalCenter
+      }
+    }
+
+    MouseArea {
+      id: trackMouse
+      anchors.fill: parent
+      hoverEnabled: true
+      cursorShape: trackRow.playable ? Qt.PointingHandCursor : Qt.ArrowCursor
+      Accessible.role: Accessible.ListItem
+      Accessible.name: trackRow.titleText +
+        (trackRow.detailText ? " — " + trackRow.detailText : "") +
+        (trackRow.playable ? ", play" : "")
+      onClicked: trackRow.activated()
+    }
+  }
+
   PopupCard {
     id: popup
     anchorItem: root
     bar: root.bar
     owner: root
     open: root.popupOpen
-    contentWidth: popup.fittedContentWidth(Style.space(340))
-    contentHeight: popup.fittedContentHeight(content.implicitHeight)
-
-    // Blurred artwork glow behind the card contents, macOS now-playing
-    // style. Sits as the first child of PopupCard's content holder so every
-    // layout child paints on top of it.
-    Rectangle {
-      anchors.fill: parent
-      anchors.margins: -popup.padding
-      radius: Style.cornerRadius
-      clip: true
-      color: Color.popups.background
-      visible: root.readyArtUrl !== ""
-
-      Image {
-        anchors.fill: parent
-        anchors.margins: Style.space(24)
-        source: root.readyArtUrl
-        fillMode: Image.PreserveAspectCrop
-        // Loads before the next paint, so the glow never shows a gap while
-        // the freshly adopted artwork decodes.
-        asynchronous: false
-        layer.enabled: true
-        layer.effect: MultiEffect {
-          blurEnabled: true
-          blur: 1.0
-          blurMax: 48
-          saturation: 0.5
-        }
-      }
-
-      Rectangle {
-        anchors.fill: parent
-        color: Util.alpha(Color.popups.background, 0.5)
-      }
-    }
+    padding: 0
+    contentWidth: popup.fittedContentWidth(root.artworkSize)
+    contentHeight: popup.fittedContentHeight(content.implicitHeight, root.miniMaxHeight)
 
     Column {
       id: content
-      anchors.fill: parent
-      spacing: Style.space(12)
+      width: parent.width
+      spacing: 0
 
-      Row {
+      Item {
+        id: artStage
         width: parent.width
-        spacing: Style.space(12)
+        height: width
+        clip: true
 
-        BorderSurface {
-          width: Style.space(88)
-          height: width
-          radius: Style.spacing.labelGap
-          color: Style.normalFillFor(root.popupForeground, Color.accent)
-          borderSpec: Border.controlSpec("normal", root.popupForeground, Color.accent)
+        Image {
+          id: artwork
+          anchors.fill: parent
+          source: root.readyArtUrl
+          fillMode: Image.PreserveAspectCrop
+          asynchronous: true
+          cache: String(source).indexOf("https:") === 0
+          smooth: true
+          mipmap: true
+          sourceSize.width: Math.round(width * Screen.devicePixelRatio)
+          sourceSize.height: Math.round(height * Screen.devicePixelRatio)
+          visible: status === Image.Ready
+        }
+
+        Rectangle {
+          anchors.fill: parent
+          visible: artwork.status !== Image.Ready
+          color: Color.popups.background
 
           Image {
-            id: artwork
-            anchors.fill: parent
-            anchors.margins: Style.space(2)
-            source: root.readyArtUrl
-            fillMode: Image.PreserveAspectCrop
+            id: miniLogoSource
+            anchors.centerIn: parent
+            width: Style.space(64)
+            height: width
+            source: Qt.resolvedUrl("assets/apple-music-monochrome.svg")
+            sourceSize.width: Math.round(width * Screen.devicePixelRatio)
+            sourceSize.height: Math.round(height * Screen.devicePixelRatio)
+            fillMode: Image.PreserveAspectFit
             asynchronous: true
-            cache: false
-            visible: status === Image.Ready
+            visible: false
+            layer.enabled: true
           }
 
-          Text {
-            anchors.centerIn: parent
-            visible: artwork.status !== Image.Ready
-            text: "󰝚"
-            color: root.popupForeground
-            font.family: root.popupFontFamily
-            font.pixelSize: Style.font.displayLarge
+          MultiEffect {
+            anchors.fill: miniLogoSource
+            source: miniLogoSource
+            colorization: 1.0
+            colorizationColor: root.popupForeground
           }
         }
 
+        MouseArea {
+          id: artClick
+          anchors.fill: parent
+          hoverEnabled: true
+          cursorShape: Qt.PointingHandCursor
+          onClicked: root.openFullPlayer()
+        }
+
+        Rectangle {
+          anchors.fill: parent
+          color: Util.alpha(Color.popups.background, artClick.containsMouse ? 0.5 : 0)
+          Behavior on color { ColorAnimation { duration: 80 } }
+
+          Text {
+            anchors.centerIn: parent
+            visible: artClick.containsMouse
+            text: root.openInNewIcon
+            color: root.popupForeground
+            font.family: root.popupFontFamily
+            font.pixelSize: Style.font.display
+          }
+        }
+      }
+
+      Column {
+        id: controlPanel
+        width: parent.width
+        topPadding: Style.space(10)
+        leftPadding: Style.space(14)
+        rightPadding: Style.space(14)
+        bottomPadding: Style.space(12)
+        spacing: Style.space(10)
+
         Column {
-          width: parent.width - Style.space(100)
-          anchors.verticalCenter: parent.verticalCenter
-          spacing: Style.space(4)
+          width: controlPanel.width - controlPanel.leftPadding - controlPanel.rightPadding
+          spacing: Style.space(2)
 
           Text {
             width: parent.width
@@ -361,7 +556,9 @@ BarWidget {
 
           Text {
             width: parent.width
-            text: root.music ? root.music.artist : ""
+            text: root.music && root.music.artist
+              ? root.music.artist
+              : (root.music && root.music.album ? root.music.album : "")
             textFormat: Text.PlainText
             visible: text !== ""
             color: Qt.darker(root.popupForeground, 1.3)
@@ -369,352 +566,325 @@ BarWidget {
             font.pixelSize: Style.font.body
             elide: Text.ElideRight
           }
-
-          Text {
-            width: parent.width
-            text: root.music ? root.music.album : ""
-            textFormat: Text.PlainText
-            visible: text !== ""
-            color: Qt.darker(root.popupForeground, 1.6)
-            font.family: root.popupFontFamily
-            font.pixelSize: Style.font.caption
-            elide: Text.ElideRight
-          }
         }
-      }
 
-      Column {
-        width: parent.width
-        spacing: Style.space(4)
-        visible: !!root.music && root.music.available
-
-        Rectangle {
-          id: progressTrack
-          width: parent.width
-          height: Style.space(5)
-          radius: height / 2
-          color: Util.alpha(root.popupForeground, 0.2)
+        Column {
+          width: controlPanel.width - controlPanel.leftPadding - controlPanel.rightPadding
+          spacing: Style.space(4)
 
           Rectangle {
-            width: parent.width * (root.music ? root.music.progress : 0)
-            height: parent.height
-            radius: parent.radius
-            color: Color.accent
+            id: progressTrack
+            width: parent.width
+            height: Style.space(6)
+            radius: height / 2
+            color: Util.alpha(root.popupForeground, 0.2)
+
+            Rectangle {
+              width: parent.width * (root.music ? root.music.progress : 0)
+              height: parent.height
+              radius: parent.radius
+              color: root.popupForeground
+            }
+
+            MouseArea {
+              anchors.fill: parent
+              enabled: !!root.music && root.music.canSeek
+              cursorShape: enabled ? Qt.PointingHandCursor : Qt.ArrowCursor
+              onClicked: function(mouse) { root.music.seekFraction(mouse.x / width) }
+            }
           }
 
-          MouseArea {
-            anchors.fill: parent
-            enabled: !!root.music && root.music.canSeek
-            cursorShape: enabled ? Qt.PointingHandCursor : Qt.ArrowCursor
-            onClicked: function(mouse) { root.music.seekFraction(mouse.x / width) }
+          Row {
+            width: parent.width
+
+            Text {
+              id: elapsed
+              text: root.music ? root.music.elapsedText : "0:00"
+              color: Qt.darker(root.popupForeground, 1.4)
+              font.family: root.popupFontFamily
+              font.pixelSize: Style.font.caption
+            }
+
+            Item { width: parent.width - elapsed.implicitWidth - duration.implicitWidth; height: 1 }
+
+            Text {
+              id: duration
+              text: root.music ? root.music.lengthText : "--:--"
+              color: Qt.darker(root.popupForeground, 1.4)
+              font.family: root.popupFontFamily
+              font.pixelSize: Style.font.caption
+            }
           }
         }
 
         Row {
-          width: parent.width
+          anchors.horizontalCenter: parent.horizontalCenter
+          spacing: Style.space(8)
 
-          Text {
-            id: elapsed
-            text: root.music ? root.music.elapsedText : "0:00"
-            color: Qt.darker(root.popupForeground, 1.4)
-            font.family: root.popupFontFamily
-            font.pixelSize: Style.font.caption
+          MiniButton {
+            iconText: "󰒮"
+            iconSize: Style.font.iconLarge
+            horizontalPadding: Style.space(12)
+            verticalPadding: Style.space(8)
+            enabled: !!root.music && root.music.available && root.music.activePlayer.canGoPrevious
+            tooltipText: "Previous"
+            onClicked: root.music.previous()
           }
 
-          Item { width: parent.width - elapsed.implicitWidth - duration.implicitWidth; height: 1 }
+          MiniButton {
+            iconText: root.playbackIcon
+            iconSize: Style.font.heading
+            horizontalPadding: Style.space(16)
+            verticalPadding: Style.space(10)
+            enabled: !!root.music && root.music.available
+            tooltipText: root.music && root.music.playing ? "Pause" : "Play"
+            onClicked: root.music.togglePlayback()
+          }
 
-          Text {
-            id: duration
-            text: root.music ? root.music.lengthText : "--:--"
-            color: Qt.darker(root.popupForeground, 1.4)
-            font.family: root.popupFontFamily
-            font.pixelSize: Style.font.caption
+          MiniButton {
+            iconText: "󰒭"
+            iconSize: Style.font.iconLarge
+            horizontalPadding: Style.space(12)
+            verticalPadding: Style.space(8)
+            enabled: !!root.music && root.music.available && root.music.activePlayer.canGoNext
+            tooltipText: "Next"
+            onClicked: root.music.next()
           }
         }
 
-        Text {
-          width: parent.width
-          visible: !!root.music && !root.music.hasValidLength
-          text: "Apple Music does not expose track duration"
-          horizontalAlignment: Text.AlignHCenter
-          color: Qt.darker(root.popupForeground, 1.6)
-          font.family: root.popupFontFamily
-          font.pixelSize: Style.font.caption
+        Row {
+          anchors.horizontalCenter: parent.horizontalCenter
+          spacing: Style.space(6)
+
+          MiniButton {
+            iconText: root.music && root.music.rating === "like"
+              ? root.heartIcon : root.heartOutlineIcon
+            iconSize: Style.font.icon
+            active: !!root.music && root.music.rating === "like"
+            enabled: !!root.music && root.music.bridgeActive
+            tooltipText: "Like"
+            onClicked: root.music.toggleLike()
+          }
+
+          MiniButton {
+            iconText: root.music && root.music.rating === "dislike"
+              ? root.thumbDownIcon : root.thumbDownOutlineIcon
+            iconSize: Style.font.icon
+            active: !!root.music && root.music.rating === "dislike"
+            enabled: !!root.music && root.music.bridgeActive
+            tooltipText: "Suggest less"
+            onClicked: root.music.toggleDislike()
+          }
+
+          MiniButton {
+            iconText: !root.music || root.music.libraryState === "unknown" ? root.plusIcon
+              : root.music.libraryState === "absent" ? root.plusIcon
+              : root.music.libraryState === "adding" ? root.clockOutlineIcon
+              : root.music.libraryState === "present" ? root.checkIcon : root.alertIcon
+            iconSize: Style.font.icon
+            active: !!root.music && root.music.libraryState === "present"
+            enabled: !!root.music && root.music.libraryState === "absent"
+            tooltipText: !root.music || root.music.libraryState === "unknown"
+              ? "Library state unavailable"
+              : root.music.libraryState === "absent" ? "Add to library"
+              : root.music.libraryState === "adding" ? "Adding to library"
+              : root.music.libraryState === "present" ? "In your library"
+              : "Couldn't add to library"
+            onClicked: root.music.addToLibrary()
+          }
+
+          Rectangle {
+            width: 1
+            height: Style.space(18)
+            anchors.verticalCenter: parent.verticalCenter
+            visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
+            color: Util.alpha(root.popupForeground, 0.18)
+          }
+
+          MiniButton {
+            visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
+            iconText: root.shuffleIcon
+            iconSize: Style.font.icon
+            active: !!root.music && root.music.shuffleMode === true
+            enabled: !!root.music && root.music.shuffleMode !== null
+            tooltipText: "Shuffle"
+            onClicked: root.music.setShuffle(root.music.shuffleMode !== true)
+          }
+
+          MiniButton {
+            visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
+            iconText: !root.music || root.music.repeatMode === "all" ? root.repeatIcon
+              : root.music.repeatMode === "one" ? root.repeatOnceIcon : root.repeatOffIcon
+            iconSize: Style.font.icon
+            active: !!root.music && (root.music.repeatMode === "all" || root.music.repeatMode === "one")
+            enabled: !!root.music && root.music.repeatMode !== "unknown"
+            tooltipText: root.music && root.music.repeatMode === "one"
+              ? "Repeat one" : root.music && root.music.repeatMode === "all" ? "Repeat all" : "Repeat off"
+            onClicked: root.music.cycleRepeat()
+          }
+
+          MiniButton {
+            visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
+            iconText: root.autoplayIcon
+            iconSize: Style.font.icon
+            active: !!root.music && root.music.autoplay === true
+            enabled: !!root.music && root.music.autoplay !== null
+            tooltipText: "Autoplay"
+            onClicked: root.music.setAutoplay(root.music.autoplay !== true)
+          }
+        }
+
+        Row {
+          anchors.horizontalCenter: parent.horizontalCenter
+          spacing: Style.space(12)
+
+          Text {
+            text: root.systemFont ? "System font on" : "System font"
+            color: Qt.darker(root.popupForeground, root.systemFont ? 1.1 : 1.6)
+            font.family: root.popupFontFamily
+            font.pixelSize: Style.font.caption
+            anchors.verticalCenter: parent.verticalCenter
+            Accessible.role: Accessible.Button
+            Accessible.name: root.systemFont ? "Turn off system font" : "Use Omarchy system font"
+
+            MouseArea {
+              anchors.fill: parent
+              hoverEnabled: true
+              cursorShape: Qt.PointingHandCursor
+              onClicked: root.toggleSystemFont()
+            }
+          }
+
+          Text {
+            text: "Art " + root.artworkSize
+            color: Qt.darker(root.popupForeground, 1.6)
+            font.family: root.popupFontFamily
+            font.pixelSize: Style.font.caption
+            anchors.verticalCenter: parent.verticalCenter
+          }
+
+          Text {
+            text: "−"
+            color: root.popupForeground
+            font.family: root.popupFontFamily
+            font.pixelSize: Style.font.body
+            anchors.verticalCenter: parent.verticalCenter
+            Accessible.role: Accessible.Button
+            Accessible.name: "Smaller artwork"
+
+            MouseArea {
+              anchors.fill: parent
+              anchors.margins: -Style.space(4)
+              hoverEnabled: true
+              cursorShape: Qt.PointingHandCursor
+              onClicked: root.setArtworkSize(root.artworkSize - 10)
+            }
+          }
+
+          Text {
+            text: "+"
+            color: root.popupForeground
+            font.family: root.popupFontFamily
+            font.pixelSize: Style.font.body
+            anchors.verticalCenter: parent.verticalCenter
+            Accessible.role: Accessible.Button
+            Accessible.name: "Larger artwork"
+
+            MouseArea {
+              anchors.fill: parent
+              anchors.margins: -Style.space(4)
+              hoverEnabled: true
+              cursorShape: Qt.PointingHandCursor
+              onClicked: root.setArtworkSize(root.artworkSize + 10)
+            }
+          }
         }
       }
 
-      Row {
-        anchors.horizontalCenter: parent.horizontalCenter
+      Flickable {
+        id: extrasFlick
+        width: parent.width
+        clip: true
+        boundsBehavior: Flickable.StopAtBounds
+        visible: extrasColumn.visible
+        height: {
+          if (!extrasColumn.visible) return 0
+          var extras = extrasColumn.implicitHeight
+          var used = artStage.height + controlPanel.implicitHeight
+          var budget = Math.max(0, root.miniMaxHeight - used)
+          return Math.min(extras, budget)
+        }
+        contentWidth: width
+        contentHeight: extrasColumn.implicitHeight
+
+      Column {
+        id: extrasColumn
+        width: extrasFlick.width
+        visible: (root.showQueue && !!root.music && root.music.upNext.length > 0)
+          || (root.showRecentlyPlayed && !!root.music && root.music.recentTracks.length > 0)
+        topPadding: 0
+        leftPadding: Style.space(10)
+        rightPadding: Style.space(10)
+        bottomPadding: Style.space(12)
         spacing: Style.space(8)
 
-        Button {
-          width: Style.space(44)
-          height: Style.space(40)
-          iconText: "󰒮"
-          foreground: root.popupForeground
-          enabled: !!root.music && root.music.available && root.music.activePlayer.canGoPrevious
-          opacity: enabled ? 1 : 0.4
-          onClicked: root.music.previous()
-        }
+        Column {
+          width: extrasColumn.width - extrasColumn.leftPadding - extrasColumn.rightPadding
+          spacing: Style.space(4)
+          visible: root.showQueue && !!root.music && root.music.upNext.length > 0
 
-        Button {
-          width: Style.space(44)
-          height: Style.space(40)
-          iconText: root.playbackIcon
-          foreground: root.popupForeground
-          iconSize: Style.font.iconLarge
-          enabled: !!root.music && root.music.available
-          opacity: enabled ? 1 : 0.4
-          onClicked: root.music.togglePlayback()
-        }
+          PanelSeparator { foreground: root.popupForeground }
 
-        Button {
-          width: Style.space(44)
-          height: Style.space(40)
-          iconText: "󰒭"
-          foreground: root.popupForeground
-          enabled: !!root.music && root.music.available && root.music.activePlayer.canGoNext
-          opacity: enabled ? 1 : 0.4
-          onClicked: root.music.next()
-        }
-      }
+          PanelSectionHeader {
+            text: "Up next"
+            foreground: root.popupForeground
+            fontFamily: root.popupFontFamily
+          }
 
-      // Compact secondary controls: ratings/library and playback modes share
-      // one row, separated visually, so the popup keeps transport prominent
-      // without spending three full rows on nine actions.
-      Row {
-        anchors.horizontalCenter: parent.horizontalCenter
-        spacing: Style.space(6)
+          Repeater {
+            model: root.showQueue && root.music ? root.music.upNext.slice(0, 6) : []
 
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          iconText: root.music && root.music.rating === "like"
-            ? root.heartIcon : root.heartOutlineIcon
-          foreground: root.music && root.music.rating === "like"
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.bridgeActive
-          opacity: enabled ? 1 : 0.4
-          tooltipText: "Like"
-          Accessible.name: "Like current song"
-          onClicked: root.music.toggleLike()
-        }
-
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          iconText: root.music && root.music.rating === "dislike"
-            ? root.thumbDownIcon : root.thumbDownOutlineIcon
-          foreground: root.music && root.music.rating === "dislike"
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.bridgeActive
-          opacity: enabled ? 1 : 0.4
-          tooltipText: "Suggest less"
-          Accessible.name: "Suggest less like the current song"
-          onClicked: root.music.toggleDislike()
-        }
-
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          iconText: !root.music || root.music.libraryState === "unknown" ? root.plusIcon
-            : root.music.libraryState === "absent" ? root.plusIcon
-            : root.music.libraryState === "adding" ? root.clockOutlineIcon
-            : root.music.libraryState === "present" ? root.checkIcon : root.alertIcon
-          foreground: root.music && root.music.libraryState === "present"
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.libraryState === "absent"
-          opacity: enabled ? 1 : 0.4
-          tooltipText: !root.music || root.music.libraryState === "unknown"
-            ? "Library state unavailable"
-            : root.music.libraryState === "absent" ? "Add to library"
-            : root.music.libraryState === "adding" ? "Adding to library"
-            : root.music.libraryState === "present" ? "In your library"
-            : "Couldn't add to library"
-          Accessible.name: !root.music || root.music.libraryState === "unknown"
-            ? "Library state unavailable"
-            : root.music.libraryState === "absent" ? "Add current song to library"
-            : root.music.libraryState === "adding" ? "Adding current song to library"
-            : root.music.libraryState === "present" ? "Current song is in your library"
-            : "Couldn't add to library"
-          onClicked: root.music.addToLibrary()
-        }
-
-        Rectangle {
-          width: 1
-          height: Style.space(20)
-          anchors.verticalCenter: parent.verticalCenter
-          visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
-          color: Util.alpha(root.popupForeground, 0.18)
-        }
-
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
-          iconText: root.shuffleIcon
-          foreground: root.music && root.music.shuffleMode === true
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.shuffleMode !== null
-          opacity: enabled ? 1 : 0.4
-          tooltipText: "Shuffle"
-          Accessible.name: "Shuffle"
-          onClicked: root.music.setShuffle(root.music.shuffleMode !== true)
-        }
-
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
-          iconText: !root.music || root.music.repeatMode === "all" ? root.repeatIcon
-            : root.music.repeatMode === "one" ? root.repeatOnceIcon : root.repeatOffIcon
-          foreground: root.music && (root.music.repeatMode === "all" || root.music.repeatMode === "one")
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.repeatMode !== "unknown"
-          opacity: enabled ? 1 : 0.4
-          tooltipText: root.music && root.music.repeatMode === "one"
-            ? "Repeat one" : root.music && root.music.repeatMode === "all" ? "Repeat all" : "Repeat off"
-          Accessible.name: tooltipText
-          onClicked: root.music.cycleRepeat()
-        }
-
-        Button {
-          width: Style.space(36)
-          height: Style.space(34)
-          visible: root.showPlaybackModes && !!root.music && root.music.bridgeActive
-          iconText: root.autoplayIcon
-          foreground: root.music && root.music.autoplay === true
-            ? Color.accent : root.popupForeground
-          enabled: !!root.music && root.music.autoplay !== null
-          opacity: enabled ? 1 : 0.4
-          tooltipText: "Autoplay"
-          Accessible.name: "Autoplay"
-          onClicked: root.music.setAutoplay(root.music.autoplay !== true)
-        }
-      }
-
-      Column {
-        width: parent.width
-        spacing: Style.space(4)
-        visible: root.showQueue && !!root.music && root.music.upNext.length > 0
-
-        PanelSeparator { foreground: root.popupForeground }
-
-        Text {
-          width: parent.width
-          text: "Up next"
-          color: Qt.darker(root.popupForeground, 1.4)
-          font.family: root.popupFontFamily
-          font.pixelSize: Style.font.caption
-          font.bold: true
-        }
-
-        Repeater {
-          model: root.showQueue && root.music ? root.music.upNext.slice(0, 6) : []
-
-          Item {
-            id: queueRow
-            required property var modelData
-            width: parent.width
-            height: queueLabel.implicitHeight + Style.space(4)
-
-            Text {
-              id: queueLabel
-              anchors.verticalCenter: parent.verticalCenter
-              width: parent.width - queueDuration.implicitWidth - Style.space(10)
-              text: queueRow.modelData.title +
-                (queueRow.modelData.artist ? " — " + queueRow.modelData.artist : "")
-              textFormat: Text.PlainText
-              color: root.popupForeground
-              font.family: root.popupFontFamily
-              font.pixelSize: Style.font.caption
-              elide: Text.ElideRight
-            }
-
-            Text {
-              id: queueDuration
-              anchors.right: parent.right
-              anchors.verticalCenter: parent.verticalCenter
-              text: root.music && queueRow.modelData.durationSeconds > 0
-                ? Model.formatTime(queueRow.modelData.durationSeconds) : ""
-              color: Qt.darker(root.popupForeground, 1.6)
-              font.family: root.popupFontFamily
-              font.pixelSize: Style.font.caption
-            }
-
-            MouseArea {
-              anchors.fill: parent
-              cursorShape: Qt.PointingHandCursor
-              onClicked: root.music.jumpToQueueIndex(queueRow.modelData.index)
+            TrackRow {
+              required property var modelData
+              titleText: modelData.title || ""
+              detailText: modelData.artist || ""
+              trailingText: root.music && modelData.durationSeconds > 0
+                ? Model.formatTime(modelData.durationSeconds) : ""
+              onActivated: root.music.jumpToQueueIndex(modelData.index)
             }
           }
         }
-      }
 
-      Column {
-        width: parent.width
-        spacing: Style.space(4)
-        visible: root.showRecentlyPlayed && !!root.music && root.music.recentTracks.length > 0
+        Column {
+          width: extrasColumn.width - extrasColumn.leftPadding - extrasColumn.rightPadding
+          spacing: Style.space(4)
+          visible: root.showRecentlyPlayed && !!root.music && root.music.recentTracks.length > 0
 
-        PanelSeparator { foreground: root.popupForeground }
+          PanelSeparator { foreground: root.popupForeground }
 
-        Text {
-          width: parent.width
-          text: "Recently played"
-          color: Qt.darker(root.popupForeground, 1.4)
-          font.family: root.popupFontFamily
-          font.pixelSize: Style.font.caption
-          font.bold: true
-        }
+          PanelSectionHeader {
+            text: "Recently played"
+            foreground: root.popupForeground
+            fontFamily: root.popupFontFamily
+          }
 
-        Repeater {
-          model: root.showRecentlyPlayed && root.music
-            ? root.music.recentTracks.slice(0, 5) : []
+          Repeater {
+            model: root.showRecentlyPlayed && root.music
+              ? root.music.recentTracks.slice(0, 5) : []
 
-          Item {
-            id: historyRow
-            required property var modelData
-            width: parent.width
-            height: historyLabel.implicitHeight + Style.space(4)
-
-            Accessible.role: Accessible.ListItem
-            Accessible.name: historyRow.modelData.title +
-              (historyRow.modelData.artist ? " — " + historyRow.modelData.artist : "") +
-              (historyRow.modelData.play ? ", replay" : ", open Apple Music")
-
-            Text {
-              id: historyLabel
-              anchors.verticalCenter: parent.verticalCenter
-              width: parent.width
-              text: historyRow.modelData.title +
-                (historyRow.modelData.artist ? " — " + historyRow.modelData.artist : "")
-              textFormat: Text.PlainText
-              color: Qt.darker(root.popupForeground, 1.3)
-              font.family: root.popupFontFamily
-              font.pixelSize: Style.font.caption
-              elide: Text.ElideRight
-            }
-            MouseArea {
-              anchors.fill: parent
-              // Replayable rows carry an exact-song descriptor; legacy rows
-              // keep the pre-existing focus behavior and must not imply replay.
-              cursorShape: historyRow.modelData.play
-                ? Qt.PointingHandCursor : Qt.ArrowCursor
-              onClicked: if (root.music) {
-                if (historyRow.modelData.play) root.music.replayTrack(historyRow.modelData)
-                else root.music.openAppleMusic()
+            TrackRow {
+              required property var modelData
+              titleText: modelData.title || ""
+              detailText: modelData.artist || ""
+              playable: !!modelData.play
+              onActivated: if (root.music) {
+                if (modelData.play) root.music.replayTrack(modelData)
+                else root.openFullPlayer()
               }
             }
           }
         }
       }
-
-      Button {
-        anchors.horizontalCenter: parent.horizontalCenter
-        text: "Open Apple Music"
-        iconText: ""
-        foreground: root.popupForeground
-        onClicked: if (root.music) root.music.openAppleMusic()
       }
     }
   }
diff --git a/extension/chromium/duration-bridge.js b/extension/chromium/duration-bridge.js
index c97d961..1dfb914 100644
--- a/extension/chromium/duration-bridge.js
+++ b/extension/chromium/duration-bridge.js
@@ -185,6 +185,33 @@ function playDescriptor(music, descriptor) {
     .catch(function() { return { ok: false, error: "play-failed" } })
 }
 
+// MusicKit artwork.url is a template (`{w}x{h}bb.jpg`) or a small CDN size.
+// The shell upgrades the size; here we only pick the best source string.
+function artworkUrlFromItem(item) {
+  var artwork = item && item.attributes && item.attributes.artwork
+  var url = artwork && artwork.url ? String(artwork.url) : ""
+  return url
+}
+
+function artworkUrlFromMediaSession(metadata) {
+  var list = metadata && metadata.artwork
+  if (!list || !list.length) return ""
+  var best = ""
+  var bestArea = -1
+  for (var i = 0; i < list.length; i++) {
+    var src = list[i] && list[i].src ? String(list[i].src) : ""
+    if (!src) continue
+    var sizes = String(list[i].sizes || "")
+    var match = sizes.match(/(\d+)\s*[x×]\s*(\d+)/i)
+    var area = match ? Number(match[1]) * Number(match[2]) : 0
+    if (area > bestArea) {
+      bestArea = area
+      best = src
+    }
+  }
+  return best
+}
+
 function normalizeQueueEntry(item, index) {
   var attributes = item && item.attributes
   if (!attributes || !attributes.name) return null
@@ -460,6 +487,7 @@ function collectBridgeState() {
       repeat: normalizeRepeat(music.repeatMode),
       autoplay: normalizeAutoplay(music.autoplayEnabled),
       play: current ? playbackDescriptorOf(current) : null,
+      artworkUrl: artworkUrlFromItem(current) || artworkUrlFromMediaSession(metadata),
       library: current && trackId && libraryCache.id === trackId
         ? libraryCache.state : "unknown"
     }
@@ -500,6 +528,13 @@ function setRating(music, item, value) {
 function handleCommand(payload) {
   try {
     var action = payload && payload.action
+    if (action === "set-system-font") {
+      var enabled = payload && payload.enabled === true
+      if (typeof window !== "undefined") window.__omarchySystemFontEnabled = enabled
+      writeSystemFont(enabled)
+      applySystemFont()
+      return Promise.resolve({ ok: true })
+    }
     var music = window.MusicKit && window.MusicKit.getInstance
       ? window.MusicKit.getInstance() : null
     if (!music) return Promise.resolve({ ok: false, error: "no-musickit" })
@@ -578,8 +613,218 @@ function handleCommand(payload) {
   }
 }
 
+var PAGE_CHROME_CSS =
+  "html,body{scrollbar-width:none !important;}" +
+  "*{scrollbar-width:none !important;}" +
+  "::-webkit-scrollbar{width:0 !important;height:0 !important;display:none !important;}" +
+  ".navigation-items--personalized .navigation-items__header{display:flex;align-items:center;gap:8px;cursor:pointer;}" +
+  ".omarchy-playlists-collapsed .navigation-items__list{display:none !important;}" +
+  // Linux has no Music.app; this CTA opens itms:// via xdg-open.
+  "[data-testid=\"native-cta\"],.native-cta{display:none !important;}" +
+  // Top Picks / mix tiles loop HLS via amp-ambient-video. Under Wayland +
+  // 75% page zoom those GPU video textures smear. Keep the static poster.
+  "amp-ambient-video,.editorial-video video,[data-testid=\"ambient-video-player\"]{display:none !important;}" +
+  // Default sidebar is 260px; pull it in ~32px.
+  "[data-testid=\"header\"].header,nav.navigation[data-testid=\"navigation\"]{width:228px !important;max-width:228px !important;min-width:0 !important;flex-basis:228px !important;}"
+
+function hideAppleMusicScrollbars() {
+  if (typeof document === "undefined") return
+  var style = document.getElementById("omarchy-apple-music-hide-scrollbars")
+  if (!style) {
+    style = document.createElement("style")
+    style.id = "omarchy-apple-music-hide-scrollbars"
+    var host = document.head || document.documentElement
+    if (host) host.appendChild(style)
+  }
+  if (style) style.textContent = PAGE_CHROME_CSS
+}
+
+function quietAmbientVideos() {
+  if (typeof document === "undefined") return
+  var nodes = document.querySelectorAll("amp-ambient-video, [data-testid='ambient-video-player']")
+  for (var i = 0; i < nodes.length; i++) {
+    nodes[i].removeAttribute("playing")
+    nodes[i].setAttribute("data-playing", "false")
+    var video = nodes[i].querySelector("video")
+    if (!video) continue
+    try {
+      video.pause()
+      video.removeAttribute("src")
+      video.load()
+    } catch (_) {}
+  }
+}
+
+function suppressNativeAppCta() {
+  if (typeof document === "undefined" || window.__omarchyNativeCtaGuard) return
+  window.__omarchyNativeCtaGuard = true
+  document.addEventListener("click", function(event) {
+    var target = event.target
+    if (!target || !target.closest) return
+    if (target.closest(".native-cta, [data-testid='native-cta']")) {
+      event.preventDefault()
+      event.stopPropagation()
+    }
+  }, true)
+}
+
+var PLAYLISTS_COLLAPSED_KEY = "omarchy-apple-music-playlists-collapsed"
+
+function readPlaylistsCollapsed(storage) {
+  try {
+    return String((storage || localStorage).getItem(PLAYLISTS_COLLAPSED_KEY) || "") === "1"
+  } catch (_) {
+    return false
+  }
+}
+
+function writePlaylistsCollapsed(collapsed, storage) {
+  try {
+    (storage || localStorage).setItem(PLAYLISTS_COLLAPSED_KEY, collapsed ? "1" : "0")
+  } catch (_) {}
+}
+
+function playlistsSection(root) {
+  var doc = root || (typeof document !== "undefined" ? document : null)
+  if (!doc || !doc.querySelectorAll) return null
+  var sections = doc.querySelectorAll(".navigation-items--personalized")
+  for (var i = 0; i < sections.length; i++) {
+    var header = sections[i].querySelector(".navigation-items__header")
+    var label = header && (header.textContent || "").trim().split("\n")[0]
+    if (label && /^playlists$/i.test(label)) return sections[i]
+  }
+  return null
+}
+
+function applyPlaylistsCollapsed(section, collapsed) {
+  if (!section) return
+  if (collapsed) section.classList.add("omarchy-playlists-collapsed")
+  else section.classList.remove("omarchy-playlists-collapsed")
+  var header = section.querySelector(".navigation-items__header")
+  if (header) header.setAttribute("aria-expanded", collapsed ? "false" : "true")
+  var toggle = section.querySelector("[data-omarchy-playlists-toggle]")
+  if (toggle) {
+    toggle.textContent = collapsed ? "▸" : "▾"
+    toggle.setAttribute("aria-label", collapsed ? "Show playlists" : "Hide playlists")
+  }
+}
+
+function ensurePlaylistsToggle() {
+  if (typeof document === "undefined") return false
+  var section = playlistsSection(document)
+  if (!section) return false
+  var header = section.querySelector(".navigation-items__header")
+  var list = section.querySelector(".navigation-items__list")
+  if (!header || !list) return false
+  applyPlaylistsCollapsed(section, readPlaylistsCollapsed())
+  if (header.getAttribute("data-omarchy-playlists-ready") === "1") return true
+  header.setAttribute("data-omarchy-playlists-ready", "1")
+  header.setAttribute("role", "button")
+  var toggle = document.createElement("button")
+  toggle.type = "button"
+  toggle.setAttribute("data-omarchy-playlists-toggle", "1")
+  toggle.style.cssText = "margin-inline-start:auto;background:none;border:none;color:inherit;cursor:pointer;opacity:0.7;font-size:12px;line-height:1;padding:0 4px;"
+  header.appendChild(toggle)
+  applyPlaylistsCollapsed(section, readPlaylistsCollapsed())
+  header.addEventListener("click", function(event) {
+    if (event.target && event.target.closest && event.target.closest("a")) return
+    event.preventDefault()
+    var next = !section.classList.contains("omarchy-playlists-collapsed")
+    writePlaylistsCollapsed(next)
+    applyPlaylistsCollapsed(section, next)
+  })
+  return true
+}
+
+function watchPlaylistsToggle() {
+  ensurePlaylistsToggle()
+}
+
+var SYSTEM_FONT_KEY = "omarchy-apple-music-system-font"
+
+function readSystemFont(storage) {
+  if (typeof window !== "undefined") {
+    if (window.__omarchySystemFontEnabled === true) return true
+    if (window.__omarchySystemFontEnabled === false) return false
+  }
+  try {
+    return String((storage || localStorage).getItem(SYSTEM_FONT_KEY) || "") === "1"
+  } catch (_) {
+    return false
+  }
+}
+
+function writeSystemFont(enabled, storage) {
+  try {
+    (storage || localStorage).setItem(SYSTEM_FONT_KEY, enabled ? "1" : "0")
+  } catch (_) {}
+}
+
+function systemFontFamily() {
+  var name = typeof window !== "undefined" ? window.__omarchyFontFamily : ""
+  return String(name || "").trim() || "system-ui"
+}
+
+// SF Pro at 13px vs a typical Omarchy mono: shrink ~8% so sidebar labels and
+// lockup titles keep similar visual mass after the family swap (same idea as
+// Grok Bot's inject, plus a scale factor).
+function systemFontCSS(family) {
+  var quoted = JSON.stringify(family || systemFontFamily())
+  return (
+    "html.omarchy-system-font{" +
+      "font-family:" + quoted + ", ui-monospace, monospace !important;" +
+      "font-size: 92% !important;" +
+    "}" +
+    "html.omarchy-system-font body," +
+    "html.omarchy-system-font button," +
+    "html.omarchy-system-font input," +
+    "html.omarchy-system-font textarea," +
+    "html.omarchy-system-font select{" +
+      "font-family: inherit !important;" +
+    "}" +
+    "html.omarchy-system-font *{" +
+      "font-family:" + quoted + ", ui-monospace, monospace !important;" +
+      "letter-spacing: -0.01em;" +
+    "}" +
+    "html.omarchy-system-font .navigation-item__link:hover{" +
+      "background-color: color-mix(in srgb, currentColor 16%, transparent) !important;" +
+    "}"
+  )
+}
+
+function applySystemFont() {
+  if (typeof document === "undefined") return
+  var on = readSystemFont()
+  document.documentElement.classList.toggle("omarchy-system-font", on)
+  var style = document.getElementById("omarchy-system-font-style")
+  if (!on) {
+    if (style) style.textContent = ""
+    return
+  }
+  if (!style) {
+    style = document.createElement("style")
+    style.id = "omarchy-system-font-style"
+    var host = document.head || document.documentElement
+    if (host) host.appendChild(style)
+  }
+  style.textContent = systemFontCSS(systemFontFamily())
+}
+
+function watchSystemFontMenu() {
+  applySystemFont()
+}
+
 if (typeof window !== "undefined" && typeof document !== "undefined") {
-  setInterval(bridgeAppleMusicDuration, 1000)
+  hideAppleMusicScrollbars()
+  suppressNativeAppCta()
+  quietAmbientVideos()
+  watchPlaylistsToggle()
+  watchSystemFontMenu()
+  setInterval(function() {
+    bridgeAppleMusicDuration()
+    ensurePlaylistsToggle()
+    quietAmbientVideos()
+  }, 1000)
   document.addEventListener("visibilitychange", bridgeAppleMusicDuration)
   window.addEventListener("pageshow", bridgeAppleMusicDuration)
 
@@ -617,6 +862,19 @@ if (typeof module !== "undefined") {
     queueContext: queueContext,
     upNextEntries: upNextEntries,
     collectBridgeState: collectBridgeState,
+    artworkUrlFromItem: artworkUrlFromItem,
+    artworkUrlFromMediaSession: artworkUrlFromMediaSession,
+    hideAppleMusicScrollbars: hideAppleMusicScrollbars,
+    PAGE_CHROME_CSS: PAGE_CHROME_CSS,
+    suppressNativeAppCta: suppressNativeAppCta,
+    PLAYLISTS_COLLAPSED_KEY: PLAYLISTS_COLLAPSED_KEY,
+    readPlaylistsCollapsed: readPlaylistsCollapsed,
+    writePlaylistsCollapsed: writePlaylistsCollapsed,
+    playlistsSection: playlistsSection,
+    SYSTEM_FONT_KEY: SYSTEM_FONT_KEY,
+    readSystemFont: readSystemFont,
+    writeSystemFont: writeSystemFont,
+    systemFontCSS: systemFontCSS,
     handleCommand: handleCommand
   }
 }
diff --git a/extension/chromium/manifest.json b/extension/chromium/manifest.json
index 092b302..e21c4f1 100644
--- a/extension/chromium/manifest.json
+++ b/extension/chromium/manifest.json
@@ -1,7 +1,7 @@
 {
   "manifest_version": 3,
   "name": "Omarchy Apple Music Bridge",
-  "version": "0.2.0",
+  "version": "0.3.0",
   "description": "Bridges Apple Music Web state (duration, rating, queue) to the Omarchy bar and relays bar commands into the page.",
   "content_scripts": [
     {
@@ -13,6 +13,15 @@
       ],
       "run_at": "document_idle",
       "world": "MAIN"
+    },
+    {
+      "matches": [
+        "https://music.apple.com/*"
+      ],
+      "js": [
+        "zoom.js"
+      ],
+      "run_at": "document_start"
     }
   ]
 }
diff --git a/extension/chromium/zoom.js b/extension/chromium/zoom.js
new file mode 100644
index 0000000..5c7005f
--- /dev/null
+++ b/extension/chromium/zoom.js
@@ -0,0 +1,7 @@
+// Isolated-world script: Chromium page zoom (Ctrl +/-), not CSS zoom.
+(function () {
+  if (typeof chrome === "undefined" || !chrome.tabs || typeof chrome.tabs.setZoom !== "function") {
+    return
+  }
+  chrome.tabs.setZoom(0.75)
+})()
diff --git a/manifest.json b/manifest.json
index 67957ed..a6c88a9 100644
--- a/manifest.json
+++ b/manifest.json
@@ -2,7 +2,7 @@
   "schemaVersion": 1,
   "id": "iuliansafta.apple-music",
   "name": "Apple Music",
-  "version": "0.3.3",
+  "version": "0.3.4",
   "author": "Iulian Safta",
   "description": "Apple Music web player controls and now-playing information for the Omarchy bar.",
   "homepage": "https://github.com/iuliansafta/omarchy-apple-music",
@@ -28,7 +28,9 @@
       "maxLabelWidth": 220,
       "showQueue": true,
       "showPlaybackModes": true,
-      "showRecentlyPlayed": true
+      "showRecentlyPlayed": true,
+      "systemFont": false,
+      "artworkSize": 260
     },
     "schema": [
       {
@@ -71,6 +73,23 @@
         "type": "boolean",
         "label": "Show recently played",
         "defaultValue": true
+      },
+      {
+        "key": "systemFont",
+        "type": "boolean",
+        "label": "Use Omarchy system font",
+        "defaultValue": false,
+        "description": "Apply the current Omarchy font to the Apple Music window, with a slightly stronger sidebar hover."
+      },
+      {
+        "key": "artworkSize",
+        "type": "integer",
+        "label": "Miniplayer artwork size",
+        "min": 180,
+        "max": 420,
+        "step": 10,
+        "defaultValue": 260,
+        "description": "Square cover size in the popup, in pixels. 260 matches the album-page artwork in the Apple Music web player."
       }
     ]
   }
diff --git a/scripts/apple-music b/scripts/apple-music
index ed6f8fb..413cd9d 100755
--- a/scripts/apple-music
+++ b/scripts/apple-music
@@ -14,6 +14,8 @@ EXTENSION_DIR="$PLUGIN_DIR/extension/chromium"
 ICON_SOURCE="$PLUGIN_DIR/assets/apple-music.svg"
 ICON_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/icons/hicolor/scalable/apps"
 ICON_FILE="$ICON_DIR/$APP_ID.svg"
+FONT_EXT_DIR="${XDG_DATA_HOME:-$HOME/.local/share}/$APP_ID/font-ext"
+PREFS_FILE="${XDG_DATA_HOME:-$HOME/.local/share}/$APP_ID/prefs.json"
 
 window() {
   hyprctl clients -j 2>/dev/null | jq -c --arg class "$APP_CLASS" \
@@ -27,12 +29,22 @@ pid() {
 }
 
 focus() {
-  local client address
+  local client address workspace name
   client=$(window || true)
   [[ -n $client ]] || return 1
   address=$(jq -r '.address' <<<"$client")
-  hyprctl dispatch "hl.dsp.focus({ window = \"address:$address\" })" >/dev/null 2>&1 ||
-    hyprctl dispatch focuswindow "address:$address" >/dev/null
+  name=$(jq -r '.workspace.name' <<<"$client")
+  workspace=$(hyprctl activeworkspace -j 2>/dev/null | jq -r '.id')
+  # Always pull it onto the current tiled workspace. Focusing a scratchpad
+  # window only reveals the overlay, which does not feel like a normal open.
+  if [[ ${workspace:-} != "" && ${workspace:-} != "null" ]]; then
+    hyprctl dispatch movetoworkspace "$workspace,address:$address" >/dev/null 2>&1 ||
+      hyprctl dispatch "hl.dsp.window.move({ workspace = \"$workspace\", window = \"address:$address\" })" >/dev/null 2>&1 ||
+      true
+  fi
+  hyprctl dispatch focuswindow "address:$address" >/dev/null 2>&1 ||
+    hyprctl dispatch "hl.dsp.focus({ window = \"address:$address\" })" >/dev/null 2>&1 ||
+    true
 }
 
 browser() {
@@ -46,16 +58,134 @@ browser() {
   fi
 }
 
+ensure_font_ext() {
+  local font
+  font=$(omarchy font current 2>/dev/null || true)
+  [[ -n $font ]] || return 0
+  mkdir -p "$FONT_EXT_DIR"
+  python3 - "$FONT_EXT_DIR" "$font" "$PREFS_FILE" <<'PY'
+import json, pathlib, sys
+directory = pathlib.Path(sys.argv[1])
+font = sys.argv[2]
+prefs_path = pathlib.Path(sys.argv[3])
+enabled = False
+if prefs_path.is_file():
+    try:
+        enabled = json.loads(prefs_path.read_text(encoding="utf-8")).get("systemFont") is True
+    except Exception:
+        enabled = False
+(directory / "manifest.json").write_text(json.dumps({
+    "manifest_version": 3,
+    "name": "Omarchy font name",
+    "version": "1.0.0",
+    "content_scripts": [{
+        "matches": ["https://music.apple.com/*"],
+        "js": ["font-name.js"],
+        "run_at": "document_start",
+        "world": "MAIN"
+    }]
+}, indent=2) + "\n", encoding="utf-8")
+(directory / "font-name.js").write_text(
+    "window.__omarchyFontFamily = " + json.dumps(font) + ";\n"
+    "window.__omarchySystemFontEnabled = " + ("true" if enabled else "false") + ";\n",
+    encoding="utf-8",
+)
+PY
+}
+
+set_font_pref() {
+  local enabled=false
+  case "${1:-}" in
+    on | true | 1) enabled=true ;;
+  esac
+  mkdir -p "$(dirname "$PREFS_FILE")"
+  python3 - "$PREFS_FILE" "$enabled" <<'PY'
+import json, pathlib, sys
+path = pathlib.Path(sys.argv[1])
+enabled = sys.argv[2] == "true"
+data = {}
+if path.is_file():
+    try:
+        data = json.loads(path.read_text(encoding="utf-8"))
+    except Exception:
+        data = {}
+if not isinstance(data, dict):
+    data = {}
+data["systemFont"] = enabled
+path.write_text(json.dumps(data, indent=2) + "\n", encoding="utf-8")
+PY
+  ensure_font_ext
+}
+
+extension_load_path() {
+  if [[ -f $FONT_EXT_DIR/manifest.json ]]; then
+    printf '%s,%s\n' "$EXTENSION_DIR" "$FONT_EXT_DIR"
+  else
+    printf '%s\n' "$EXTENSION_DIR"
+  fi
+}
+
+ensure_default_zoom() {
+  python3 - "$PROFILE_DIR/Default/Preferences" <<'PY'
+import json, math, os, sys
+path = sys.argv[1]
+os.makedirs(os.path.dirname(path), exist_ok=True)
+level = math.log(0.75) / math.log(1.2)
+data = {}
+if os.path.exists(path):
+    try:
+        with open(path, encoding="utf-8") as handle:
+            data = json.load(handle)
+    except Exception:
+        data = {}
+if not isinstance(data, dict):
+    data = {}
+partition = data.setdefault("partition", {})
+if not isinstance(partition, dict):
+    partition = {}
+    data["partition"] = partition
+zoom = partition.get("default_zoom_level")
+if not isinstance(zoom, dict) or not zoom:
+    partition["default_zoom_level"] = {"x": level}
+else:
+    partition["default_zoom_level"] = {key: level for key in zoom}
+hosts = partition.get("per_host_zoom_levels")
+if not isinstance(hosts, dict):
+    hosts = {}
+    partition["per_host_zoom_levels"] = hosts
+bucket_key = next(iter(hosts), "x")
+bucket = hosts.get(bucket_key)
+if not isinstance(bucket, dict):
+    bucket = {}
+    hosts[bucket_key] = bucket
+bucket["music.apple.com"] = level
+tmp = path + ".tmp"
+with open(tmp, "w", encoding="utf-8") as handle:
+    json.dump(data, handle, separators=(",", ":"))
+os.replace(tmp, path)
+PY
+}
+
 open_app() {
   if focus; then
     return
   fi
 
   mkdir -p "$PROFILE_DIR"
+  ensure_default_zoom
+  ensure_font_ext
+  # Occlusion / backgrounding pauses Apple Music's <audio> when the window
+  # is on the scratchpad or another workspace. These flags keep playback
+  # going; Hyprland render_unfocused on this class is the compositor half.
   exec setsid uwsm-app -- "$(browser)" \
     --ozone-platform=wayland \
+    --disable-backgrounding-occluded-windows \
+    --disable-renderer-backgrounding \
+    --disable-background-timer-throttling \
+    --disable-features=CalculateNativeWinOcclusion,IntensiveWakeUpThrottling \
+    --hide-scrollbars \
     --user-data-dir="$PROFILE_DIR" \
-    --load-extension="$EXTENSION_DIR" \
+    --load-extension="$(extension_load_path)" \
     --remote-debugging-port=0 \
     --app="$APP_URL"
 }
@@ -96,9 +226,11 @@ case "${1:-open}" in
   focus) focus ;;
   pid) pid ;;
   install) install_launcher ;;
+  refresh-font) ensure_font_ext ;;
+  set-font-pref) set_font_pref "${2:-off}" ;;
   is-installed) [[ -f $DESKTOP_FILE ]] ;;
   *)
-    echo "Usage: $0 {open|focus|pid|install|is-installed}" >&2
+    echo "Usage: $0 {open|focus|pid|install|refresh-font|set-font-pref|is-installed}" >&2
     exit 2
     ;;
 esac
diff --git a/scripts/bridge-daemon b/scripts/bridge-daemon
index ffccf12..edb705b 100755
--- a/scripts/bridge-daemon
+++ b/scripts/bridge-daemon
@@ -418,6 +418,7 @@ def sanitize_state(value):
         "repeat": _text(value.get("repeat"), 16),
         "library": _text(value.get("library"), 32),
         "play": sanitize_descriptor(value.get("play")),
+        "artworkUrl": _text(value.get("artworkUrl"), 2048),
     }
     # Tri-state fields: forwarded only when definitely boolean, so the UI
     # keeps rendering "unknown" instead of a fabricated off.
diff --git a/tests/bridge_daemon.test.py b/tests/bridge_daemon.test.py
index 4dcd127..67ce129 100755
--- a/tests/bridge_daemon.test.py
+++ b/tests/bridge_daemon.test.py
@@ -69,6 +69,15 @@ class SanitizeStateTest(unittest.TestCase):
         self.assertEqual(state["queuePosition"], 0)
         self.assertEqual(state["queueLength"], 3)
 
+    def test_artwork_url_is_forwarded_and_capped(self):
+        url = "https://is1-ssl.mzstatic.com/image/thumb/Music/{w}x{h}bb.jpg"
+        state = bridge_daemon.sanitize_state({"ok": True, "artworkUrl": url})
+        self.assertEqual(state["artworkUrl"], url)
+        state = bridge_daemon.sanitize_state({"ok": True, "artworkUrl": "x" * 5000})
+        self.assertEqual(len(state["artworkUrl"]), 2048)
+        state = bridge_daemon.sanitize_state({"ok": True, "artworkUrl": 12})
+        self.assertEqual(state["artworkUrl"], "")
+
     def test_upnext_is_whitelisted_and_capped(self):
         items = [{"index": i, "id": "i", "title": "t", "artist": "a",
                   "durationSeconds": 1, "evil": "x" * 100000} for i in range(500)]
diff --git a/tests/duration-bridge.test.js b/tests/duration-bridge.test.js
index b8c71e4..aff5967 100644
--- a/tests/duration-bridge.test.js
+++ b/tests/duration-bridge.test.js
@@ -55,6 +55,41 @@ assert.equal(entry.index, 3)
 assert.equal(entry.id, "song-1")
 assert.equal(entry.title, "Up Next Song")
 assert.equal(entry.durationSeconds, 200)
+assert.equal(
+  bridge.artworkUrlFromItem({ attributes: { artwork: { url: "https://is1-ssl.mzstatic.com/{w}x{h}bb.jpg" } } }),
+  "https://is1-ssl.mzstatic.com/{w}x{h}bb.jpg")
+assert.equal(bridge.artworkUrlFromItem({ attributes: {} }), "")
+assert.equal(
+  bridge.artworkUrlFromMediaSession({ artwork: [
+    { src: "https://cdn.example/small.jpg", sizes: "60x60" },
+    { src: "https://cdn.example/large.jpg", sizes: "600x600" }
+  ] }),
+  "https://cdn.example/large.jpg")
+assert.equal(typeof bridge.hideAppleMusicScrollbars, "function")
+assert.equal(bridge.PAGE_CHROME_CSS.indexOf("native-cta") !== -1, true)
+assert.equal(bridge.PAGE_CHROME_CSS.indexOf("amp-ambient-video") !== -1, true)
+assert.equal(bridge.PAGE_CHROME_CSS.indexOf("228px") !== -1, true)
+bridge.hideAppleMusicScrollbars()
+const store = {
+  data: {},
+  getItem: function(key) { return Object.prototype.hasOwnProperty.call(this.data, key) ? this.data[key] : null },
+  setItem: function(key, value) { this.data[key] = String(value) }
+}
+assert.equal(bridge.readPlaylistsCollapsed(store), false)
+bridge.writePlaylistsCollapsed(true, store)
+assert.equal(store.data[bridge.PLAYLISTS_COLLAPSED_KEY], "1")
+assert.equal(bridge.readPlaylistsCollapsed(store), true)
+bridge.writePlaylistsCollapsed(false, store)
+assert.equal(bridge.readPlaylistsCollapsed(store), false)
+assert.equal(bridge.readSystemFont(store), false)
+bridge.writeSystemFont(true, store)
+assert.equal(bridge.readSystemFont(store), true)
+const fontCss = bridge.systemFontCSS("iA Writer Mono S")
+assert.equal(fontCss.indexOf("iA Writer Mono S") !== -1, true)
+assert.equal(fontCss.indexOf("92%") !== -1, true)
+assert.equal(fontCss.indexOf("navigation-item__link:hover") !== -1, true)
+const fontCmd = bridge.handleCommand({ action: "set-system-font", enabled: true })
+assert.equal(typeof fontCmd.then, "function")
 assert.equal(bridge.normalizeQueueEntry(null, 0), null)
 assert.equal(bridge.normalizeQueueEntry({ attributes: {} }, 0), null)
 
diff --git a/tests/model.test.js b/tests/model.test.js
index 7124e40..23209da 100644
--- a/tests/model.test.js
+++ b/tests/model.test.js
@@ -35,12 +35,33 @@ assert.equal(model.upNextFromState({ ok: true }).length, 0)
 // or process argv. Markup delimiters and controls cannot trigger rich text.
 assert.equal(model.metadataText("Song <b>title</b>\nnext"), "Song ‹b›title‹/b› next")
 assert.equal(model.metadataText("x".repeat(600)).length, 512)
+assert.equal(model.upgradeArtworkUrl(
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/{w}x{h}bb.jpg"),
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/1200x1200bb.jpg")
+assert.equal(model.upgradeArtworkUrl(
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/60x60bb.jpg"),
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/1200x1200bb.jpg")
+assert.equal(model.upgradeArtworkUrl(
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/1400x1400bb.webp"),
+  "https://is1-ssl.mzstatic.com/image/thumb/Music/1400x1400bb.webp")
 assert.equal(model.artworkUrl("https://example.com/cover.jpg"), "https://example.com/cover.jpg")
+assert.equal(model.artworkUrl(
+  "https://is1-ssl.mzstatic.com/image/thumb/Music112/v4/ab/cd/ef/source/100x100bb.jpg"),
+  "https://is1-ssl.mzstatic.com/image/thumb/Music112/v4/ab/cd/ef/source/1200x1200bb.jpg")
 assert.equal(model.artworkUrl("file:///tmp/cover.jpg"), "file:///tmp/cover.jpg")
+assert.equal(model.artworkUrl("file:///tmp/.org.chromium.Chromium.cover"),
+  "file:///tmp/.org.chromium.Chromium.cover")
 assert.equal(model.artworkUrl("http://example.com/cover.jpg"), "")
 assert.equal(model.artworkUrl("data:image/png;base64,AAAA"), "")
 assert.equal(model.artworkUrl("file://remote/cover.jpg"), "")
 assert.equal(model.artworkUrl("https://example.com/" + "x".repeat(2048)), "")
+assert.equal(model.artworkUrl("file:///usr/lib/chromium/product_logo_256.png"), "")
+assert.equal(model.artworkUrl("file:///usr/share/icons/hicolor/256x256/apps/chromium.png"), "")
+assert.equal(model.artworkUrl("https://www.gstatic.com/chrome/product_logo.png"), "")
+assert.equal(model.historyField([], "title"), "")
+assert.equal(model.historyField([{ title: "Held", artist: "Band", art: "file:///tmp/a.jpg" }], "title"), "Held")
+assert.equal(model.historyField([{ title: "Held", artist: "Band", art: "file:///tmp/a.jpg" }], "artist"), "Band")
+assert.equal(model.historyField([{ title: "Held", art: "file:///usr/lib/chromium/product_logo_256.png" }], "art"), "")
 
 const historyLines = [
   JSON.stringify({ ts: 1, title: "A", artist: "X" }),
